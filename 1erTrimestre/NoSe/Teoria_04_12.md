querySelector: document.querySelector() -> es un método que permite seleccionar el 
primer elemento que coincida con un selector CSS especificado dentro del documento HTML.

Toma como argumento un selector CSS